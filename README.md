# Dewey
Dewey is the secure write-gateway container for the Trophy Backlog Command Center. It isolates the master SQLite database from the AI tier, converting cryptographically signed JSON approvals into parameterized SQL.
